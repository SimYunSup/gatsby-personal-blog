---
import type { StarlightPageProps } from "@astrojs/starlight/props";
import StarlightPage from "@astrojs/starlight/components/StarlightPage.astro";
import { ChatInterface } from '../../components/about/ChatInterface';
import '../../styles/app.css';

export function getStaticPaths() {
  return [
    {
      params: { lang: 'ko' },
    }
  ]
}

type Frontmatter = StarlightPageProps['frontmatter'];
const frontmatter = {
  title: 'About Me',
  template: 'splash',
  hero: {
    title: '',
  },
} satisfies Frontmatter;
---

<StarlightPage frontmatter={frontmatter}>
  <main class="min-h-screen flex flex-col" data-pagefind-ignore>
    <header class="text-center py-12 px-6 text-white" style="background: linear-gradient(to bottom right, var(--color-primary), var(--color-primary));">
      <h1 class="text-4xl font-bold mb-4">About Me</h1>
      <p class="text-lg opacity-95">
        사용자 경험과 개발 생산성을 숫자로 증명하는 프론트엔드 개발자
      </p>
    </header>

    <section class="flex-1 p-8 max-w-6xl mx-auto w-full">
      <ChatInterface client:load />
    </section>
  </main>
</StarlightPage>
